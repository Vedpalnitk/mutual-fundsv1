# =============================================================
# Sparrow Invest Web App — Environment Variables
# Copy to .env.local and adjust as needed:  cp .env.example .env.local
# =============================================================

# ---- Backend API proxy target ----
# Next.js rewrites /api/* to this URL (see next.config.js).
BACKEND_URL=http://localhost:3801

# ---- Subdomain-based portal separation (optional) ----
# When set, middleware enforces portal boundaries per hostname.
# When unset (local dev), all portals are accessible from any host.
# NEXT_PUBLIC_APP_HOSTNAME=app.sparrow-invest.com
# NEXT_PUBLIC_ADMIN_HOSTNAME=admin.sparrow-invest.com
